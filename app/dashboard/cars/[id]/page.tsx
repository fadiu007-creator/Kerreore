"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { ArrowLeft, Save, Trash2 } from "lucide-react";
import { useParams } from "next/navigation";
import { createClient } from "@/lib/supabase/browser";
export default function ManageCar(){const{id}=useParams<{id:string}>();const supabase=createClient();const[c,setC]=useState<any>(null);const[error,setError]=useState("");const[busy,setBusy]=useState(false);useEffect(()=>{(async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user){location.href="/login";return}const{data,error}=await supabase.from("kerreore_vehicles").select("*").eq("id",id).eq("owner_id",user.id).single();if(error)location.href="/provider";else setC(data)})()},[id]);if(!c)return <main className="grid min-h-screen place-items-center">Loading…</main>;async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const{error}=await supabase.from("kerreore_vehicles").update({make:f.get("make"),model:f.get("model"),year:Number(f.get("year")),hourly_rate:Number(f.get("rate")),location:f.get("location"),description:f.get("description"),transmission:f.get("transmission"),fuel:f.get("fuel"),seats:Number(f.get("seats"))}).eq("id",id);if(error)setError(error.message);else setError("Saved. Because you edited the listing, it will require admin review again.");setBusy(false)}async function remove(){if(!confirm("Delete this vehicle and its bookings?"))return;const{error}=await supabase.from("kerreore_vehicles").delete().eq("id",id);if(error)setError(error.message);else location.href="/provider"}return <main className="min-h-screen bg-[#f7f7f4]"><header className="border-b border-black/8 bg-white"><div className="mx-auto flex max-w-4xl items-center justify-between px-5 py-5"><Link href="/provider" className="flex items-center gap-2 text-sm font-bold"><ArrowLeft size={16}/> Provider</Link><button onClick={remove} className="rounded-full border border-red-200 px-4 py-2 text-sm font-bold text-red-700"><Trash2 className="mr-1 inline" size={15}/> Delete</button></div></header><section className="mx-auto max-w-4xl px-5 py-10"><p className="text-xs font-black uppercase tracking-[.18em] text-black/40">Manage listing</p><h1 className="mt-2 text-4xl font-black">{c.make} {c.model}</h1><form onSubmit={save} className="mt-8 space-y-5 rounded-[2rem] border border-black/8 bg-white p-6"><div className="grid gap-4 md:grid-cols-2">{[["make","Make",c.make],["model","Model",c.model],["year","Year",c.year],["rate","Hourly rate (€)",c.hourly_rate],["location","Location",c.location],["seats","Seats",c.seats]].map(([n,l,v])=><label key={n} className="text-sm font-bold">{l}<input name={n} defaultValue={v as any} required type={n==="year"||n==="rate"||n==="seats"?"number":"text"} className="mt-2 w-full rounded-2xl border border-black/10 bg-[#f7f7f4] p-4"/></label>)}</div><div className="grid gap-4 md:grid-cols-2"><label className="text-sm font-bold">Transmission<select name="transmission" defaultValue={c.transmission} className="mt-2 w-full rounded-2xl border border-black/10 bg-[#f7f7f4] p-4"><option>automatic</option><option>manual</option></select></label><label className="text-sm font-bold">Fuel<select name="fuel" defaultValue={c.fuel} className="mt-2 w-full rounded-2xl border border-black/10 bg-[#f7f7f4] p-4"><option>petrol</option><option>diesel</option><option>hybrid</option><option>electric</option></select></label></div><label className="text-sm font-bold">Description<textarea name="description" defaultValue={c.description||""} rows={5} className="mt-2 w-full rounded-2xl border border-black/10 bg-[#f7f7f4] p-4"/></label>{error&&<p className="rounded-2xl bg-[#f5f5f0] p-4 text-sm font-bold">{error}</p>}<button disabled={busy} className="w-full rounded-2xl bg-black py-4 font-black text-white"><Save className="mr-2 inline" size={16}/>{busy?"Saving…":"Save changes"}</button></form></section></main>}
