"use client";
import { useState } from "react";
import { createClient } from "@/lib/supabase/browser";
export default function LoginPage(){const[mode,setMode]=useState<"login"|"signup">("login");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[name,setName]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");const supabase=createClient();if(mode==="login"){const{data,error}=await supabase.auth.signInWithPassword({email,password});if(error){setError(error.message);setBusy(false);return}const{data:profile}=await supabase.from("kerreore_profiles").select("role").eq("id",data.user.id).single();window.location.href=profile?.role==="provider"?"/provider":"/dashboard";return}const{data,error}=await supabase.auth.signUp({email,password,options:{data:{full_name:name}}});if(error){setError(error.message);setBusy(false);return}if(data.session)window.location.href="/dashboard";else{setError("Account created. Check your email to confirm your account before signing in.");setBusy(false)}}return <main className="flex min-h-screen items-center justify-center px-5 py-10"><div className="w-full max-w-md"><a href="/" className="text-3xl font-black tracking-[-.07em]">kerreore<span className="text-lime-500">.</span></a><div className="mt-10 rounded-[2rem] border border-black/8 bg-white p-7 shadow-sm"><div className="mb-7 flex rounded-2xl bg-[#f5f5f0] p-1"><button onClick={()=>{setMode("login");setError("")}} className={`flex-1 rounded-xl py-2.5 text-sm font-black ${mode==="login"?"bg-white shadow-sm":"text-black/45"}`}>Sign in</button><button onClick={()=>{setMode("signup");setError("")}} className={`flex-1 rounded-xl py-2.5 text-sm font-black ${mode==="signup"?"bg-white shadow-sm":"text-black/45"}`}>Create account</button></div><h1 className="text-3xl font-black tracking-[-.05em]">{mode==="login"?"Welcome back":"Join Kerreore"}</h1><p className="mt-2 text-sm text-black/50">{mode==="login"?"Sign in with your Kerreore account.":"Create a secure Kerreore account."}</p><form onSubmit={submit} className="mt-7 space-y-4">{mode==="signup"&&<input value={name} onChange={e=>setName(e.target.value)} required placeholder="Full name" className="w-full rounded-2xl border border-black/10 bg-[#f7f7f4] px-4 py-3.5 outline-none"/>}<input value={email} onChange={e=>setEmail(e.target.value)} required type="email" placeholder="Email address" className="w-full rounded-2xl border border-black/10 bg-[#f7f7f4] px-4 py-3.5 outline-none"/><input value={password} onChange={e=>setPassword(e.target.value)} required minLength={8} type="password" placeholder="Password (8+ characters)" className="w-full rounded-2xl border border-black/10 bg-[#f7f7f4] px-4 py-3.5 outline-none"/>{error&&<p className="rounded-xl bg-red-50 p-3 text-sm font-semibold text-red-700">{error}</p>}<button disabled={busy} className="w-full rounded-2xl bg-black py-3.5 font-black text-white disabled:opacity-50">{busy?"Please wait…":mode==="login"?"Sign in":"Create account"}</button></form><p className="mt-5 text-center text-xs text-black/40">Authentication and sessions are handled by Supabase Auth. Roles are enforced by the database, not browser storage.</p></div></div></main>}