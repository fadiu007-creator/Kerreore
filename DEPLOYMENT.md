# Kerreore deployment

This file intentionally records the production deployment trigger for the GitHub → Vercel integration.

The admin and provider routes are present under `app/admin` and `app/provider` and should be included in the next production build.
